#include "cards.h"

using namespace std; 

int main()
{   cards::restoreCardName();   // Aufruf der Funktion zur Kartenreparatur
    cards::repairStatus();      // Ausgabe der Anzahl der reparierten Karten
    return 0;
}